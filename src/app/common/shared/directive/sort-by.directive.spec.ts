import { SortByDirective } from './sort-by.directive';

describe('SortByDirective', () => {
  it('should create an instance', () => {
    const directive = new SortByDirective();
    expect(directive).toBeTruthy();
  });
});
